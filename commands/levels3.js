const Discord = require("discord.js");


module.exports.helpDesc = new Discord.RichEmbed()
  .setAuthor("Levels:")
  .setTitle("-levels")
  .setColor(0xb5111c)
  .setDescription("This command lists the top 3 users on the server, and thier stats.")
  .addField("Example:", "`-levels`")
  .setTimestamp()



exports.run = (client, message) => {











}